# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Authentication Secret
JWT_SECRET=koncepthive_super_secret_jwt_key_2026

# PostgreSQL Database Configuration (Optional: system automatically falls back to SQLite if PostgreSQL connection fails)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=taskmanager_db
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/taskmanager_db
